Features to implement:

_ fix discuss page tabs
  instead of 'view', we need 'page' -
  or maybe it would be easier if discuss were a side link, not a tab
_ export
_ history
_ diffs
_ file upload
_ atom feed (site)
_ atom feed (page)
_ config file [started]
_ categories
_ site initialization
_ caching
_ cache expiration program?
_ plugins/interface
_ user logins/accounts
