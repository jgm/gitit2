Features to implement:

_ fix slowness:  culprit is the new call to latest in
  getRawContents.  We should only call this when we
  actually need it (as with gitit).

x atom feed (site)
x atom feed (page)
_ export [started]
_ file upload [started]
_ config file [started]
_ metadata [started]
_ categories
_ site initialization
_ caching
_ cache expiration program?
_ plugins/interface
_ user pages (linked from history)
_ user logins/accounts
